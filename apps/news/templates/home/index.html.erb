<h1>home#index</h1>

<p>hash: <%= params[:hash] %></p>
<p><%= time %></p>

<hr>

<% params.env.each do |key,val| %>
    <p>params.env['<%= key %>'] = "<%= val %>"</p>
<% end %>

<% ENV.each do |key,val| %>
    <p>ENV['<%= key %>'] = "<%= val %>"</p>
<% end %>
